# @package _global_
# Quick plotting test - minimal iterations for fast verification
# Usage: uv run python run_solver.py +experiment=plotting_test solver=fv
#        uv run python run_solver.py +experiment=plotting_test solver=spectral

experiment_name: Plotting-Test
sweep_name: plotting-test-sweep

Re: 100
tolerance: 1.0e-4
max_iterations: 50
