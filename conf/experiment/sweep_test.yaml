# @package _global_
# Minimal sweep test - verifies nested MLflow runs work correctly
# Usage: uv run python run_solver.py -m +experiment=sweep_test solver=fv,spectral N=8,16

experiment_name: Sweep-Test
sweep_name: test-sweep

Re: 100
tolerance: 1.0e-3
max_iterations: 50
